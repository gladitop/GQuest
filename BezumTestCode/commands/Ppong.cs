using System;
namespace BezumTestCode
{
    public partial class Commands
    {
        void command_name(object[] dd)
        {          
           // string of = ((string)dd[0]); //Переменная string из массива[string]
        }
    
    }
}